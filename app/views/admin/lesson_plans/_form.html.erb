<%= form_for(@lesson_plan) do |f| %>
  <% if @lesson_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lesson_plan.errors.count, "error") %> prohibited this lesson_plan from being saved:</h2>

      <ul>
      <% @lesson_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content, rows: 10, cols: 100 %>
  </div>
  <% if @lesson_plan.id %>
    <div class="field">
      <%= f.label :topic %><br>
      <%= f.text_field :topic, size: 100 %>
    </div>
    <div class="field">
      <%= f.label :handout_url %><br>
      <%= f.text_field :handout_url, size: 100 %>
    </div>
  <% end %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
