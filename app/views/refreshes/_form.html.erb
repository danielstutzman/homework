<%= form_for(@refresh) do |f| %>
  <% if @refresh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@refresh.errors.count, "error") %> prohibited this refresh from being saved:</h2>

      <ul>
      <% @refresh.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :repo_id %><br>
    <%= f.number_field :repo_id %>
  </div>
  <div class="field">
    <%= f.label :exercise_id %><br>
    <%= f.number_field :exercise_id %>
  </div>
  <div class="field">
    <%= f.label :logged_at %><br>
    <%= f.datetime_select :logged_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
